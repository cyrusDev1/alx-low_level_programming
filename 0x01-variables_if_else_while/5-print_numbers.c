#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Return 0
 *
 */

int main(void)
{
	int ch;

	for (ch = 0; ch <= 9; ch++)
		printf("%d", ch);
	printf("\n");
	return (0);
}
